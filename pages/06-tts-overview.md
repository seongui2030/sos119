# TTS의 원리

TTS(Text to Speech)는 글자를 사람의 목소리처럼 들리는 음성으로 바꾸는 기술입니다. 똑똑 비서는 AI 답변을 화면에 표시하는 동시에 TTS API에 보내 사용자가 들을 수 있게 합니다.

```text
AI 답변 문자열 → 음성 모델 → MP3 데이터 → 브라우저 Audio 재생
```

TTS는 서버에서 처리합니다. 브라우저는 완성된 MP3를 받아 재생하므로 API 키를 알 필요가 없습니다.
